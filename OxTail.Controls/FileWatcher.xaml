<UserControl x:Class="OxTail.Controls.FileWatcher"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" xmlns:my="clr-namespace:OxTail.Controls">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="276*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <StatusBar x:Name="statusBar" Grid.Row="1">
      <StatusBar.ItemsPanel>
        <ItemsPanelTemplate>
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="4*"/>
              <ColumnDefinition Width="Auto"/>
              <ColumnDefinition Width="Auto"/>
              <ColumnDefinition Width="Auto"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
          </Grid>
        </ItemsPanelTemplate>
      </StatusBar.ItemsPanel>
      <StatusBarItem Grid.Column="0">
        <TextBlock Name="textBlockMainStatus">Ready</TextBlock>
      </StatusBarItem>
      <StatusBarItem Grid.Column="1">
        <ComboBox Name="comboBoxNewlineDetection" Width="70"/>
      </StatusBarItem>
      <StatusBarItem Grid.Column="2">
        <ComboBox Name="comboBoxEncoding" Width="100"/>
      </StatusBarItem>
      <StatusBarItem Grid.Column="3">
        <Button x:Name="buttonRefresh" Click="buttonRefresh_Click">
          <Image Source="/OxTail.Controls;component/Images/refresh.png"></Image>
        </Button>
      </StatusBarItem>
      <StatusBarItem Grid.Column="4">
        <ProgressBar Name="progressBarStatus" Width="50" Height="18"/>
      </StatusBarItem>
    </StatusBar>
    <my:ColourfulListView x:Name="colourfulListView" Grid.Row="0" x:FieldModifier="private" FontFamily="Consolas"/>
  </Grid>
</UserControl>